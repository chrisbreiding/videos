// Generated by CoffeeScript 1.4.0
(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(['backbone', 'handlebars', 'views/subscription-search', 'collections/subscriptions', 'views/subscription'], function(Backbone, Handlebars, SubscriptionSearchView, subscriptions, SubscriptionView) {
    var SubscriptionsView;
    return SubscriptionsView = (function(_super) {

      __extends(SubscriptionsView, _super);

      function SubscriptionsView() {
        this.addAllSubs = __bind(this.addAllSubs, this);

        this.addOneSub = __bind(this.addOneSub, this);
        return SubscriptionsView.__super__.constructor.apply(this, arguments);
      }

      SubscriptionsView.prototype.el = '#subscriptions-region';

      SubscriptionsView.prototype.$subs = $('#subscriptions');

      SubscriptionsView.prototype.$editText = $('.edit-subscriptions span');

      SubscriptionsView.prototype.events = {
        'click .edit-subscriptions': 'editSubscriptions'
      };

      SubscriptionsView.prototype.initialize = function() {
        this.editingSubs = false;
        new SubscriptionSearchView;
        subscriptions.on('add', this.addOneSub);
        subscriptions.on('reset', this.addAllSubs);
        return subscriptions.fetch();
      };

      SubscriptionsView.prototype.addOneSub = function(sub) {
        var view;
        view = new SubscriptionView({
          model: sub
        });
        return this.$subs.append(view.render().el);
      };

      SubscriptionsView.prototype.addAllSubs = function() {
        this.$subs.html('');
        return subscriptions.each(this.addOneSub, this);
      };

      SubscriptionsView.prototype.editSubscriptions = function(e) {
        e.preventDefault();
        if (this.editingSubs) {
          this.$editText.html('Edit');
        } else {
          this.$editText.html('Done');
        }
        this.$subs.toggleClass('editing');
        return this.editingSubs = !this.editingSubs;
      };

      return SubscriptionsView;

    })(Backbone.View);
  });

}).call(this);
