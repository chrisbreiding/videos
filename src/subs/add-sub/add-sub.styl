.add-sub-buttons {
  .add-sub-buttons-links {
    display: flex;
    margin: 1em 0;
    padding-right: 0.4em;

    a {
      @extends .button;
      display: block;
      flex-grow: 2;
      margin-left: 0.4em;
      text-align: center;
      text-decoration: none;
    }

    .icon {
      font-size: 0.8em;
    }
  }
}

.add-sub {
  flex-grow: 2;
  padding: 2em;
}
